export const occurrenceType = (processedData) => {return {
  id             : processedData.id,
  occId          : processedData.occId,
  term           : processedData.term,
  scientificName : processedData.scientificName,
  fileId         : processedData.fileId,
  frameId        : processedData.frameId,
  sentence       : processedData.sentence,
  column         : processedData.column,
  spalte         : processedData.spalte,
}};