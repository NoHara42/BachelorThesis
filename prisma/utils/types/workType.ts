export const extractedWork = (processedData) => {return {
  fileId: processedData.fileId,
  rFrameMean: processedData.RframeMean,
  nFrameMean: processedData.NframeMean,
  hFrameMean: processedData.HframeMean,
  sFrameMean: processedData.SframeMean,
  rWork: processedData.Rwork,
  nWork: processedData.Nwork,
  hWork: processedData.Hwork,
  sWork: processedData.Swork,
  rBeta: processedData.Rbeta,
  hBeta: processedData.Hbeta,
  sBeta: processedData.Sbeta,
  rFrameMean_genDiv: processedData.RframeMean_genDiv,
  rWork_genDiv: processedData.Rwork_genDiv,
  nFrameMean_genDiv: processedData.NframeMean_genDiv,
  nWork_genDiv: processedData.Nwork_genDiv,
  vocab: processedData.vocab,
  words: processedData.words,
  rBeta_genDiv: processedData.Rbeta_genDiv,
  nBeta_genDiv: processedData.Nbeta_genDiv,
  lustrum: processedData.Lustrum,
  title: processedData.Title,
  work: processedData.Work,
  agePublication: processedData.Age_Publication,
  year: processedData.Year,
  join: processedData.join,
  split: processedData.split,
  regionX: processedData.Region_x,
  originalLanguage: processedData.Original_Language,
  literatureForm: processedData.Literature_Form,
  genreX: processedData.Genre_x,
  origin: processedData.Origin,
  mainSubject: processedData.Main_Subject,
  narrativeLocation: processedData.Narrative_Location,
  downloads: processedData.Downloads,
  size: processedData.Size,
  completeFlagX: processedData.completeFlag_x,
  literaryPeriodX: processedData.Literary_period_x,
  literatureFormV2: processedData.Literature_Form_v2
}};